#ifndef COMMON_H
#define COMMON_H

#include <cstdlib>
#include <ctime>

using namespace std;

const float objects_distance = 400.0f;
const int window_width = 550;
const int window_height = 600;
const int bonus_rarity = 7;

float random(float from, float to);

#endif //COMMON_H